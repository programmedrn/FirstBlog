---
permalink: "manifest.json"
layout: null
sitemap: false
---
{
  "lang": "{{ site.lang | default: "en-US" }}",
  "name": "{{ site.title }}",
  "short_name": "{{ site.short_name | default: site.title | replace: ' ', '' }}",
  "theme_color": "{{ site.manifest.theme_color | default: '#0b141b' }}",
  "background_color": "{{ site.manifest.background_color | default: '#0b141b' }}",
  {% if site.favicons -%}
    "icons": [
      {% for icon in site.favicons -%}
      {
        "src": "{{ icon[1] | relative_url }}",
        "sizes": "{{ icon[0] }}x{{ icon[0] }}"
      }{% if forloop.last != true %},{% endif %}
      {% endfor %}
    ],
  {% endif -%}
  "start_url": "{{ '/' | relative_url }}",
  "scope": "{{ '/' | relative_url }}",
  "display": "standalone"
}
