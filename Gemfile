source "https://rubygems.org"

# The same Jekyll and plugin versions GitHub Pages uses to build this site
gem "github-pages", group: :jekyll_plugins

gem "webrick" # Needed by `jekyll serve` on Ruby 3+
gem "faraday-retry" # Silences a retry-middleware warning from a github-pages dependency
